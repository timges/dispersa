{
  "$description": "Semantic spacing tokens — six-layer convention: category.concept.scale",
  "spacing": {
    "gap": {
      "$type": "dimension",
      "$description": "Gap spacing for flexbox/grid gaps and between-element spacing",
      "xs": { "$value": "{spacing.scale.xs}", "$description": "Extra small gap" },
      "sm": { "$value": "{spacing.scale.sm}", "$description": "Small gap" },
      "md": { "$value": "{spacing.scale.md}", "$description": "Medium gap" },
      "lg": { "$value": "{spacing.scale.lg}", "$description": "Large gap" },
      "xl": { "$value": "{spacing.scale.xl}", "$description": "Extra large gap" }
    },
    "inset": {
      "$type": "dimension",
      "$description": "Inset spacing for padding within containers",
      "xs": { "$value": "{spacing.scale.xs}", "$description": "Extra small inset" },
      "sm": { "$value": "{spacing.scale.sm}", "$description": "Small inset" },
      "md": { "$value": "{spacing.scale.md}", "$description": "Medium inset" },
      "lg": { "$value": "{spacing.scale.lg}", "$description": "Large inset" },
      "xl": { "$value": "{spacing.scale.xl}", "$description": "Extra large inset" }
    }
  }
}
