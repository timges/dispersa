{
  "name": "dispersa-examples-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:update": "vitest run --update"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
